# Neovim Config

Minimal, portable Neovim setup built step by step.

## Goals
- Works in client environments
- No dependency on Nerd Fonts
- Terminal-first workflow
- Fully understandable configuration

## Structure
- init.lua — entry point
- lua/config — core config (options, keymaps)
- lua/plugins — plugins (added later)
